Attention:
	--* The file "index.html" is the main page and it contains links for every other page.
	--* Header contains a slide show that changes about every 5 seconds.
	--*	The Persian font used in this project is "Lalehzar" and is imported from Google Fonts,
			So if you don't have an internet connection, there might be differences compared to
			original project styles, Because everything is coordinated with respect to the main font.